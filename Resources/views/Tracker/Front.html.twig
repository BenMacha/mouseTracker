<script>
    window.MouseTrackerConfig = {
        urls: {
            base: '{{ app.request.getSchemeAndHttpHost() }}',
            createClient: '{{ path('mousetracker_createClient') }}',
            addData: '{{ path('mousetracker_addData') }}',
            addTag: '{{ path('mousetracker_addTag') }}',
            clearPartial: '{{ path('mousetracker_clearPartial') }}'
        },
        settings: {{ settings|default({})|json_encode|raw }}
    };
</script>
<script defer src="{{ asset('bundles/tracker/js/tracker.js') }}"></script>
